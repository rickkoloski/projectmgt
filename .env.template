# Supabase Configuration
# --------------------------------------------------
# Find these values in your Supabase dashboard under Settings > API
# Project URL (example: https://abcdefghijklm.supabase.co)
SUPABASE_URL=https://your-project-id.supabase.co

# API Key - use the "anon" public key from Project API keys section
SUPABASE_API_KEY=your-api-key-here

# JWT Secret (for token verification)
# Find this in your Supabase dashboard under Settings > API > JWT Settings
SUPABASE_JWT_SECRET=your-jwt-secret-here

# PostgreSQL Database Connection
# --------------------------------------------------
# Format: postgres://username:password@hostname:port/database_name
# Find this in your Supabase dashboard under Project Settings > Database > Connection string > URI
DATABASE_URL=postgres://postgres:postgres@db.your-project-id.supabase.co:5432/postgres

# Optional: Individual connection parameters (only used if DATABASE_URL is not set)
DB_HOST=db.your-project-id.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-db-password

# Application Settings
# --------------------------------------------------
# Set to 'true' to enable email notifications
ENABLE_EMAIL_NOTIFICATIONS=false

# SMTP Configuration for sending emails (if enabled)
SMTP_ADDRESS=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_DOMAIN=yourdomain.com
SMTP_AUTHENTICATION=plain
SMTP_ENABLE_STARTTLS_AUTO=true

# Default sender email address
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# To use this file:
# 1. Make a copy named ".env" (without .template)
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. The .env file is gitignored, so your credentials won't be committed